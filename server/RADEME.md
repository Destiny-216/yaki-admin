# PHP Backend (Demo)

This directory contains the PHP + MySQL backend for the Yakiniku management system.

## Local setup
1. Import database/schema.sql
2. (Optional) Import database/seed.sql
3. Configure server/config/db.php (see db.php.example)
4. Run: php -S localhost:8000 -t server
