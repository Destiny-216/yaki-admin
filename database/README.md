# Database Files / データベース関連ファイル

- `schema.sql`: Database schema (table definitions)  
  （データベースのテーブル構造を定義するファイル）

- `seed.sql`: Sample data for local testing  
  （ローカル環境での動作確認用サンプルデータ）

- `debug.sql`: Used for local debugging only  
  （ローカルでのデバッグ目的のみに使用）
